[mysqld]
# 메모리 절약을 위한 InnoDB 설정 (t3.micro 기준)
innodb_buffer_pool_size = 128M
innodb_log_buffer_size  = 8M

# 커넥션 제한
max_connections         = 50

# 쿼리 캐시 비활성화 (MySQL 8.0에서 기본 제거됨)
performance_schema      = OFF