name: EveryGSM prod CD workflow

on:
  push:
    branches:
      - 'main'
  workflow_dispatch:

jobs:
  CD:
    runs-on: ubuntu-latest
    environment: prod
    steps:
      - name: Temporary Disabled Step
        run: echo "This deployment workflow is temporarily disabled."
#      - name: Checkout Code
#        uses: actions/checkout@v6
#
#      - name: Setup JDK 25
#        uses: actions/setup-java@v5
#        with:
#          java-version: '25'
#          distribution: 'temurin'
#
#      - name: Setup Gradle
#        uses: gradle/actions/setup-gradle@v5
#        with:
#          cache-read-only: false
#          gradle-home-cache-cleanup: true
#
#      - name: Make YML Files
#        run: |
#          touch ./src/main/resources/application-prod.yml
#          echo "${{ secrets.PROD_APPLICATION_YAML }}" > ./src/main/resources/application-prod.yml
#        shell: bash
#
#      - name: Build with Gradle
#        run : ./gradlew build -x test --parallel --build-cache
#
#      - name: Configure AWS Credentials
#        uses: aws-actions/configure-aws-credentials@v5
#        with:
#          aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
#          aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
#          aws-region: ${{ secrets.AWS_REGION }}
#
#      - name: Make ZIP File
#        run: zip -r ./$GITHUB_SHA.zip .
#        shell: bash
#
#      - name: Upload to S3
#        run: aws s3 cp --region ${{ secrets.AWS_REGION }} ./$GITHUB_SHA.zip s3://${{ secrets.AWS_S3_BUCKET_NAME }}/prod/$GITHUB_SHA.zip
#
#      - name: Code Deploy
#        run: aws deploy create-deployment --application-name everygsm-prod-server-codedeploy --deployment-config-name CodeDeployDefault.AllAtOnce --deployment-group-name everygsm-prod-server --s3-location bucket=${{ secrets.AWS_S3_BUCKET_NAME }},bundleType=zip,key=prod/$GITHUB_SHA.zip
#
#      - name: CD Success Notification
#        uses: sarisia/actions-status-discord@v1
#        if: success()
#        with:
#          webhook: ${{ secrets.DISCORD_INFORMATION_ALERT_CHANNEL_WEBHOOK }}
#          color: 0x4CAF50
#
#      - name: CD Failure Notification
#        uses: sarisia/actions-status-discord@v1
#        if: failure()
#        with:
#          webhook: ${{ secrets.DISCORD_INFORMATION_ALERT_CHANNEL_WEBHOOK }}
#          color: 0xFF4C4C
